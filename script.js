// Upon submit:
//      JS calculates price and show it to user.
//      Pricing: 
//          Small $7.00
//          Medium  $10.00
//          Large  $14.00
//          1 topping  $2.00
//          2 toppings $3.00

document.addEventListener("DOMContentLoaded", function (event) {
    
    
    //Size
    function handleClick(RadioElement) {   // RadioElement is the object returned from "this"
        var selectedValue = RadioElement.value;
        if (selectedValue == "yes") {
            blah
        }
        if (selectedValue == "no") {
            blah
        }
        else {
            blah   
        }
    }

    //Toppings

    //Submit
    function calculate() {
        document.querySelector('submit');
    }
}



























    });










